<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>New Entry - Activity Tracker</title>
<link rel="stylesheet" href="/styles.css">
<script>(function(){var provided='<%= theme || "light" %>';var stored=localStorage.getItem('theme');document.documentElement.dataset.theme=stored||provided;})();</script>
</head>
<body class="app">
  <%- include('partials/header', { activePage:'entry', user:user }) %>
<main class="app-main" style="grid-template-columns:1fr;">
<section class="card form-card" style="max-width:520px; margin:0 auto;">
  <h1 class="page-title" style="font-size:1.4rem;">Add New Entry</h1>
  <p class="subtle" style="margin-top:-.4rem;">Log your meters.</p>
  <form method="post" action="/entry/new" class="form-grid">
    <div class="field">
      <label for="meters">Meters</label>
      <input id="meters" name="meters" type="number" min="1" step="1" required placeholder="e.g. 1500">
    </div>
    <div class="actions">
      <button class="btn" type="submit">Save Entry</button>
      <a href="/dashboard" class="link-inline">My Progress</a>
    </div>
  </form>
</section>
</main>
<footer class="app-footer"><small>&copy; <span id="year"></span></small></footer>
<script>document.getElementById('year').textContent=new Date().getFullYear();const btn=document.getElementById('toggleTheme');btn&&btn.addEventListener('click',()=>{const next=document.documentElement.dataset.theme==='dark'?'light':'dark';document.documentElement.dataset.theme=next;localStorage.setItem('theme',next);});</script>
</body>
</html>